{
  "name": "udemy-playwright",
  "version": "1.0.0",
  "description": "Automated software testing with Playwright",
  "main": "index.js",
  "scripts": {
    "tests:smoke": "playwright test --config=smoke.config.ts",
    "tests:regression": "playwright test --config=regression.config.ts",
    "tests:all": "npm run tests:smoke && npm run tests:regression"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.49.1",
    "prettier": "^3.4.2"
  }
}
